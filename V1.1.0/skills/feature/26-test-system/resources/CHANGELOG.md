# 系统测试 Skill 版本更新日志

## v2.1.0 (2026-04-19)

- **重构**：提取公共模块 `common.py`，消除 4 个脚本间的重复 JSON 读写和路径处理代码
- **重构**：拆分 `scan_project`（95行）、`scan_security`（130行）、`generate_report`（300+行）等过长函数
- **一致性**：统一错误处理风格（`print + sys.exit(1)` 用于致命错误，返回空值用于可降级场景）
- **一致性**：统一类型注解风格（所有脚本均使用 `typing` 模块）
- **文档**：补充 CLI 参数详细说明、输出 JSON 格式、错误处理说明、完整的外部服务列表
- **测试**：新增 `test_common.py`（30 个测试），覆盖所有公共模块函数
- **测试**：新增边界条件测试（空数据、无效类型、IOError 等）
- **安全**：`run_system_tests.py` 健康检查增加路径遍历防护
- **清理**：删除意外创建的 `--cov=scripts` 文件

## v2.0.0

- 初始版本：4 个核心脚本 + 206 个单元测试 + 100% 覆盖率
